{
  "name": "backend",
  "version": "1.0.0",
  "description": "Wenas, esto es una api echa para hacer pruebas. Date cuenta que estamos usando el CORS (Cross-Origin Sources). node api.js para correrlo, accede a http://localhost:3005/api",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon api.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "standard": "^17.0.0"
  }
}
